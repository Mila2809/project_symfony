{% extends 'base.html.twig' %}

{% block title %}New Utilisateur{% endblock %}

{% block body %}
    <link rel="stylesheet" href="{{ asset('styles/sign_in.css') }}">
    <div class="groupe">
    <h1>Create new Utilisateur</h1>

    {{ include('utilisateur/_form.html.twig') }}
    <hr/>
    <a class="button" href="{{ path('app_utilisateur_index') }}">back to list</a>
    </div>
{% endblock %}
